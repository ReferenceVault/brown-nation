// Fallback values shown before the live /shipping-settings fetch resolves
// (or if it fails). The actual values are admin-editable at runtime — see
// src/lib/api/public/shippingSettings.ts.
export const DEFAULT_SHIPPING_FLAT_RATE = 49;
export const DEFAULT_FREE_SHIPPING_THRESHOLD = 999;
